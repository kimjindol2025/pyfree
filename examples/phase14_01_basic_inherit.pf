class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        print(self.name + " makes a sound")

class Dog(Animal):
    def bark(self):
        print(self.name + " says Woof!")

dog = Dog("Buddy")
dog.speak()
dog.bark()
print(dog.name)
